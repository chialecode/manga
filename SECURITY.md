# 安全策略

## 报告漏洞

**请不要通过公开 issue 报告安全漏洞。**

请使用 GitHub 的私密漏洞报告通道：本仓库 **Security** 标签页 → **Report a vulnerability**。
该通道只有仓库维护者可见，在修复发布前不会公开。

报告时请尽量包含：

- 受影响的版本与构建渠道（正式版 / 开发版）
- 复现步骤，或一个最小复现样本
- 影响范围：能读到什么、能写到什么、是否需要用户交互
- 你认为的严重程度与理由

本项目是个人维护的开源项目，没有服务等级承诺。我会尽力在收到报告后 7 天内确认，并在修复后于发布说明中致谢（除非你要求匿名）。

## 支持范围

只有最新发布版本接受安全修复。本项目尚未发布正式版本，当前处于开发阶段。

## 本项目关注的威胁

以下是设计上被视为安全边界的部分，破坏其中任何一条都属于安全漏洞：

- **渲染进程零特权**：渲染进程不应能取得 Node API、文件系统句柄、数据库连接或任何密钥。
- **路径解析**：任何绕过能力网关（`capability-gate`）读到授权媒体库根目录之外文件的方式。
- **媒体 URL 不透明**：任何让真实文件系统路径出现在 URL 中的方式。
- **电子书内容隔离**：任何让 EPUB 内嵌脚本得以执行的方式（该场景下 CSP 是唯一防线）。
- **凭证泄露**：任何让 API Key、令牌或私钥出现在日志、错误信息、IPC 响应或仓库中的方式。
- **供应链**：`vendor-bin` 二进制或更新包的完整性校验被绕过。
- **用户媒体只读**：任何代码路径写入、移动、重命名或删除用户媒体目录中的文件。

## 不在范围内

- 需要攻击者已取得本机用户账户权限的攻击。
- 用户自行安装的第三方插件或自行配置的外部数据源导致的问题。
- 未经修改的上游依赖中的漏洞——请向对应上游项目报告；同时欢迎在此告知我们以便升级。

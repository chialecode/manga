# M1a：给 Cursor 的连续执行 prompt

日期：2026-09-19。配套 [详细执行计划](m1a-execution-plan.md)；当前事实见 [M0 交付复核](../evidence/2026-09-19-m0-closure-review.md)。这是下一轮实施任务，不是对 M0 或 M1a 的验收签字。将下列文本完整交给当前仓库中的 Cursor。

```text
请从 MANGA 当前仓库和工作区继续，按 Git 交接规则先核对 M0 基线提交，再在明确基点的 M1a 目标分支连续完成“M1a 基础门槛与 Agent”实施。实际编码、修复、运行检查并交付可启动的 Windows 本地产物；不要只写计划或在首个聊天演示完成后结束。

你承担本轮 Agent B：Agent A 已制定详细计划，B 连续执行和自审，完成后由 A 集中复核。以仓库中的计划、有效决定和实际代码为依据，不依赖接收方拥有之前的聊天记录。普通实现细节自主处理，新增用户指示同步到计划后继续。

先执行 git status --short --branch，保留全部现有改动。阅读：
1. AGENTS.md、docs/README.md、docs/delivery/status.md。
2. docs/delivery/m1a-execution-plan.md：本轮范围与 P0—P5 详细验收，逐项实施。
3. docs/evidence/2026-09-19-m0-closure-review.md：当前缺陷修复、证据与剩余边界。
4. docs/decisions/confirmed-decisions.md、docs/decisions/0007-technology-stack.md、docs/decisions/open-questions.md。
5. docs/delivery/roadmap-and-acceptance.md、REVIEW.md，再按 AGENTS 触发表阅读实际受影响正本与代码。
6. docs/dev-rules/git-and-github.md 第 1 节：分支、commit、push 的时机、频率与交接。
7. docs/dev-rules/development-workflow.md 的“Agent 连续执行与集中审批”和“A 规划、B 执行、A 审查”，以及 docs/templates/agent-handoff.md：工作节奏、交接信息和真实阻塞处理。

先自动完成 Git 交接，再直接实施，不要求用户分别发“commit”“建分支”“开始”：
- 检查 M0 复核基线是否已提交。若仍在 feat/m0-tech-contract-validation 且留有本次复核成果，核对归属、执行必要检查，形成一个准确的 M0 基线 commit；提交说明保留 M0 整体未验收的限制。基线已保存则直接复用，不重复提交。
- M1a 目标分支为 feat/m1a-agent-foundation。若当前已经在该分支且继承正确基线，直接继续；否则分支不存在时从已保存的 M0 基线创建，已存在时先核对再复用，不 reset 或覆盖。若前置已合并，使用包含 M0 成果的最新 main；未合并则记录 M0 依赖，不能从缺少成果的旧 main 开始。
- 完成或识别以上步骤后直接进入 P0。本地基线提交与目标分支准备属于本轮交接范围，不是逐项审批节点；无需等 M0 push、PR 或合并才开始本地实施。

版本保存要求：P0—P5 在同一分支连续实现和自检，首次交付时保存一个实现 commit；后续同一交付未 push 的集中审查修复 amend 原提交，消息描述最终结果；已推送后默认追加集中修复。M0 与 M1a 是不同交付，即使都未 push 也分别保留 commit。不要按工作包、功能点、时间或文件数量拆提交/分支。中间仅按回退、试验分叉或中断恢复需要增加检查点。push/PR/合并等若已有明确授权按范围执行，未获授权则将具体目标、提交范围和操作放入最终集中清单，不阻塞独立本地工作；不把推送授权推导为发布授权。明确要求暂不提交时优先遵守。

连续执行要求：用户希望尽量少打断 Agent，完成整个授权范围后集中审批。常规本地实现、已选依赖安装、测试、修复、文档同步和 Git 交接直接处理；每个 P 工作包完成后自行检查并继续，不询问“是否继续”。技术候选/视觉偏好先完成可逆实现和证据，不因 draft/proposed 状态暂停全项目，也不代签 accepted。把非阻塞决定、真实 API/设备配置、人工验收与外部操作授权统一留在最终交付报告。只有缺失决定确实影响需求、数据含义、授权或不可逆结果，且无法用合理可逆方案隔离时，才提前提出具体问题；说明依据和影响，只暂停其依赖部分并继续其他工作。进度更新不是审批请求，用户沉默也不是批准。

已有事实与授权：
- 当前 M0 原型仍是 textarea、node:sqlite、Zod 3、实验构建；不能当作 React/better-sqlite3/Forge 已通过。
- 用户已确认 React/Radix/Tailwind/Lucide、Tiptap/CodeMirror、better-sqlite3/Drizzle/FTS5、Zod 4、JSZip、Vite/Forge、Vitest/Testing Library/playwright-core、pi-ai、safeStorage 等对应技术。按实际使用方接入，不重复询问，不批量添加未来功能依赖。
- M1a 先 Agent 主页面、连接和基础配置；M1b 再完整阅读/笔记/录音。TXT/EPUB/MOBI/PDF 与 M2 媒体格式是已确认目标，不得用旧拒绝矩阵缩减目标。
- 数据默认系统 Documents；全部非安装生成内容可配置，开发/正式通道隔离。不要移动或删除真实用户文件。
- 旧原型输入法/美式键盘/三档缩放、外接麦克风基础录音已获用户通过；新 React 输入界面只在最终稳定后列短人工复测。耳机/串音/硬件偏移在 M1b 有正确测量入口后完成。
- 可逆本地实施、依赖安装、合成数据与故障测试已有授权。不要自动开启真实麦克风、调用用户密钥、上传私人材料或修改系统权限。版本提交和远端操作按上述 Git 规则与会话有效授权执行。

严格按 P0—P5 顺序推进：

P0 正式工程：建立有使用方的 React + Vite/Forge Windows 产品宿主，Zod 4 契约兼容、中文消息资源与语义 Token、类型/依赖/组件烟测。保留 M0 实验和回归覆盖；禁止推倒重建或清空库。笔记阶段再引入 Tiptap/CodeMirror。新模块补模块规格与数据所有权。

P1 权限与生命周期：修复固定 scopeHandle="library" 并不等于有限授权的问题。宿主绑定 actor、任务/会话、允许命令、资源/对象读写集合、代次与撤销；模型不能自填身份或路径权限。查询/上下文/搜索/写入/导出/幂等回放均校验，范围过滤在正文返回前完成。React UI Facet、工具、上下文、service/worker 真实共用启停链；撤销、取消、晚到写入、重新启用、50 次启停与失败恢复均有确定性场景。不要以布尔 UI 标志或“成功失败均可”的断言充数。先验证 kernel 和单写入宿主候选，给 ADR-0004/0005 提供实证、维护成本与推荐；未获正式决定不得代签 accepted。

P2 存储与位置：接入 better-sqlite3/Drizzle/FTS5，验证旧库/资料包身份、修订、引用、未知对象、附件和检索范围。真实子进程退出覆盖迁移、资料包文件发布、提交和配置切换；支持恢复或回滚孤立暂存，保留原件和冲突文件。全部生成目录可配置，平台解析 Documents，release/development/测试独立；Electron 初始化前确定写入位置，持久化启动指针也可配置并可见。目录离线/只读/迁移失败有恢复；不移动仅索引的外部原件。safeStorage 保护凭据，仅受控输入可触及明文，不进状态/日志/导出。通过新驱动原生 ABI、rebuild、asar、独立包启动/恢复后再开放真实 Agent 工具。

P3 Agent 服务：按 pi-ai + 必要独立协议适配器实现 Responses、Chat Completions 的文本/流式/工具调用，以及独立 audio/transcriptions multipart。文本/转录分别连接、路由、测试能力；保留 base URI 前缀，不重复 /v1；缺模型列表允许手填。运行有步骤/时间/上下文/费用预算、稳定会话/任务/目标版本、取消/重试/恢复与工具证据。工具只来自当前启用且授权的实际能力，不注册未来空工具。源材料或模型输出中的指令不能扩大权限。至少一个资源查询和一个实际可撤销业务修改走 UI/Agent 同一服务。

P4 中文完整界面：Agent 主页与副驾驶共享会话/任务；材料、范围、工具结果、取消/重试可见。首次配置可跳过 AI，完成后可修改目录/连接/模型。资源总览的数量/占用/位置/托管与索引状态来自实际服务，异步扫描可取消，失联和缺模块有准确状态。集中中文键/插值/日期数字；覆盖空、加载、错误、离线、权限和模块关闭、键盘、焦点、窄窗与缩放。

P5 最终交付：建立本地可控 HTTP 提供者，完整验证两套文本协议、multipart、流式工具参数累积、取消、401/429、超时/断线、缺能力、恶意参数与重复晚到事件。不需要真实 API key 才能完成自动化。接入真实存在的统一检查，保留旧 node:test 覆盖直到迁移完成。源码稳定后验证新 Windows 包和当前参考机原响应目标，记录样本量、原始值与最终源码指纹；不要用 DOM 回执证明显示器延迟或用 tiny EPUB 证明 30 MiB EPUB 首屏。

不得回退本轮修复：
- contextSnapshot 使用指定资源修订，选区按 Unicode 码点，外来资源/part/非法范围拒绝。
- 拆块后 notes.update 明确 blockId，保留其他块/检索范围；结构更新同步索引。
- 资料包校验 schema/路径/字节预算/哈希/引用，空目标和不覆盖保护；恢复搜索与 noteScopes；普通失败回滚自己发布的文件并保留冲突文件；重试幂等。
- 录音原始字节保持，窗口回放无需系统 FFmpeg，独立导出可见；暂停/跳转/停止的媒体时间正确；事件使用 captureOffsetMs，不把闲置播放器或无身份样本绑到文本资源。
- 解析 worker 停用取消与旧结果拒绝，重新启用可继续导入；报告拒绝缺用例、旧指纹、失败包/性能和缺失指标。

本轮不扩展：完整阅读器/新笔记编辑器/声学采样校准归 M1b；图像长图和 MKV/HEVC/ASS/多音轨/VFR 完整播放与 Bangumi 归 M2；BitTorrent 与第三方沙箱归 M4。保留具体后续责任与验收，不把缺代码写成“等待用户测试”。M0 整体未退出不妨碍独立基础实现，但不能因此宣称 M0 或 M1a 验收通过。

文档和证据要求：
- 精确覆盖 AT-49/50/51/55 及 P1/P2 所需 POC/AT 子场景，完整 AT 不能由子集升级为 passed。
- 新结果放独立目录，尊重 M0_EVIDENCE_DIR，不覆盖历史报告。状态、计划、协议、模块规格、导航/登记联动更新。
- 仓库公开材料只用相对路径/环境变量/合成数据；不含私人内容、凭据、个人绝对路径或设备标识。
- 本地执行 node scripts/verify.mjs；M0 基线需要时按质量门禁执行 package、bench、verify-m0；新产品门禁先实现再接入，不使用 --if-present 跳过必需检查。
- 先用定向测试修复，再做最终与源码一致的检查。不得手改指纹、降低断言或仅复制旧报告。

最终待用户处理清单只保留真正尚需决定或操作的事项，每项提供推荐、证据/界面、影响与批准后动作；已解决/已授权项不再列为待批。真正阻塞决定按上文及时提出，其余合并交付，不能把缺代码或可自动测试转交用户。

交付前按计划逐项完成自审与可自动修复，在 docs/evidence/m1a-delivery.md 提供交接报告并登记/导航：按 P0—P5 及相关用例映射实现位置、实际命令/退出结果、证据、状态与限制；给出基线、目标分支、产物及依赖说明。报告随代码一次提交，最终 commit 在交付消息中给出，不为报告写回自身哈希再追加提交。已有报告保留历史证据，按新版本补充。最终聊天只需结果、commit、报告/产物入口和集中待处理事项；不要以长日志或“全部完成”替代证据。后续若收到 A 的问题编号，从指定审查版本继续，保留 A 的修复，逐条补修复和验证，不重建工程或重新实现未受影响范围。

最终答复必须包含：分支/commit 和未提交文件归属，实际执行过的 push/PR 状态及前置分支依赖；实际产物与启动步骤；P0—P5 逐项结果；修复和回归；检查命令/状态/指纹；M0、M1a 基础门槛、M1a 产品验收分别判断；一份集中待用户处理清单（人工、技术/视觉决定、未授权外部操作及推荐依据）。交付前按 REVIEW.md 自审并直接修复范围内问题。
```

# POC-04 生命周期与独立宿主

> 历史记录：以下保留 Cursor 原交付时的结果与判断。复核发现部分通过结论证据不足，当前状态、修复和补测以 [后续复核报告](2026-09-19-m0-followup-review.md) 为准；旧基准设备要求已撤销。

| 字段 | 内容 |
| --- | --- |
| 报告 ID / 日期 / 执行者 | EV-20260919-poc-04；实施 Agent |
| 关联 | POC-04；BASE-01/03、AGENT-01/05、PLUG；AT-09—11/14/17/21/37 子集 |
| 被测版本 | `docs/evidence/m0/poc-04.json` |
| 环境 | Node 24 子进程 IPC；非 Electron 窗口 |
| 样本 | 合成笔记命令 |
| 结论 | 自动化契约 passed。Electron 窗口人工检查另见检查单 |

## 步骤与结果

| 用例 / 命令 | 前提与操作 | 预期 | 实际 | 状态 / 证据 |
| --- | --- | --- | --- | --- |
| UI/Agent 同命令 | `notes.create` 两种 actor | 等价成功、不同对象 | ok + 不同 objectId | passed |
| 幂等 | 重复同一 idempotencyKey | 重放、不第二份逻辑结果 | `idempotentReplay=true` | passed |
| 50 次启停 | 阅读器 activate/deactivate | 句柄不增长 | 资源计数收敛 | passed |
| 旧 epoch | 停用后执行已封装命令 | CAPABILITY_UNAVAILABLE/EPOCH_MISMATCH | error | passed |
| 无关 registry 变化 | bump 后新命令 | 仍可用 | ok | passed |
| Node IPC | 子进程执行同一命令 | 可序列化往返 | 有结果；非法 JSON 不崩宿主 | passed |

## 限制与决定

跨进程证据来自 Node stdio JSON，不是 Electron utilityProcess。服务/UI/worker Facet 在原型中以 service 为主。

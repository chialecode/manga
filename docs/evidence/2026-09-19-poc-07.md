# POC-07 组合运行时比较

> 历史记录：以下保留 Cursor 原交付时的结果与判断。复核发现部分通过结论证据不足，当前状态、修复和补测以 [后续复核报告](2026-09-19-m0-followup-review.md) 为准；旧基准设备要求已撤销。

| 字段 | 内容 |
| --- | --- |
| 报告 ID / 日期 / 执行者 | EV-20260919-poc-07；实施 Agent |
| 关联 | POC-07；PLUG-05/06、NFR-05；AT-36—38/48 子集 |
| 被测版本 | `docs/evidence/m0/poc-07.json` |
| 环境 | 同一 SDK 与夹具 |
| 样本 | many/single、冲突 Bundle、激活失败、阅读器停用 |
| 结论 | 自研 `packages/kernel` 通过硬门槛；naive activator 泄漏失败。正式选型 proposed |

## 步骤与结果

| 用例 / 命令 | 前提与操作 | 预期 | 实际 | 状态 / 证据 |
| --- | --- | --- | --- | --- |
| many 提供者 | 启用 metadata hub | 绑定 alpha+beta | 两者在计划中 | passed |
| 显式关闭 | disabled meta-b | 不隐式启用 | 不包含 meta-b | passed |
| Bundle 冲突 | 同时启用冲突阅读器 | BUNDLE_CONFLICT | 抛错 | passed |
| 阅读器停用 | 停用后计数 | 库数据保留 | resources 不变 | passed |
| 激活失败 | activate 抛错 | failed + 无残留 | failed | passed |
| naive 候选 | 停用不 dispose | 硬门槛失败 | leaked=true | passed（作为淘汰证据） |

## 限制与决定

未开发第二套完整内核。筛选记录：不引入第三方微内核/Electron 模块加载器，因其不能直接表达提交屏障与 epoch。推荐生产只保留 `manga-kernel`。热停用范围：JS 模块停用有效；原生替换仍应重启。

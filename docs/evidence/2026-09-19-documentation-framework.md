# 文档框架验证记录

> 公开整理说明（2026-09-19）：设备标识与本机专属信息已脱敏；测试数据、状态和当时结论保留，未重新执行实验。文中的旧待决范围仅代表报告时点，当前决定见 [确认记录](../decisions/confirmed-decisions.md)。

| 字段 | 内容 |
| --- | --- |
| 报告 ID / 日期 | EV-20260919-documentation-framework / 2026-09-19 |
| 执行者 | 本次文档维护 Agent |
| 关联 | ADR-0001；用户要求完善 MANGA 文档约束管理框架 |
| 工作范围 | 根入口、文档分层、规则、决定与执行台账、模板、原文档接线和文档校验 |
| 环境 | Windows、PowerShell 7、Node.js 24.19.0；无应用工程或 Git 元数据 |
| 被测版本 | 下文记录工作目录内容摘要；结构检查结果不覆盖后续修改 |
| 结论 | passed：文档结构、需求追踪及检查器故障检出；不代表应用验收 |

## 初次框架验证记录

以下结果与内容摘要对应框架建立时的历史快照；后续修订的当前结果见[文档整理复核](2026-09-19-documentation-revision.md)。

| 检查 | 实际结果 |
| --- | --- |
| `node --check scripts/check-docs.mjs` | 退出码 0，语法检查通过 |
| `node scripts/check-docs.mjs` | 退出码 0；登记 39 份 Markdown、核对 237 个本地链接、62 项需求、48 个 AT、9 个 POC |
| 检查器反例 | 临时目录隔离复制，11 种故障全部被检出；每次恢复原文件后再运行下一项 |
| 正向语法夹具 | 基线、显式锚点、引用式链接定义、代码围栏内假链接/假 ID 均按约定处理 |
| 原文档差异复核 | 10 份原文档修改；需求与技术正文保留，补治理入口、首发确认、假设迁移和状态/证据接线 |
| 新增文件核对 | 新增 29 份 Markdown、1 份登记 JSON、1 个标准库检查脚本 |

故障样本依次为：不存在的链接、不存在的锚点、逃逸仓库的相对路径、未定义 AT、删除一条需求映射、重复需求编号、遗漏 POC 执行登记、非法文档状态、非法日期、CLAUDE 与 AGENTS 分叉，以及未登记的新增文档。样本只修改临时副本，未污染工作文档。

重现方法：用 Node.js 在临时目录复制项目，导入 `scripts/check-docs.mjs` 的 `validateRepository(directory)`；先断言基线 `errors` 为空，再分别注入上述故障，断言出现相应诊断并恢复原文。CLI 与该函数共用检查逻辑，返回非零表示结构失败。文档链接存在不证明对应目标已实现。

所有产品 POC/AT 仍未执行。A-01/A-02 按本次真实用户回复转为已确认；Q-01/Q-02 尚未回复，保持现有建议顺序和 M0 选型假设。

## 被测内容摘要

SHA-256：`6e2911e776d0ee74393facb93b3c9aa50ee2df39ecef9b7899d22dbcca41f712`（40 个文件，不含本报告）。计算范围为按仓库相对路径排序的全部文件，排除此报告以避免自引用，每个文件依次写入 UTF-8 路径、NUL、文件原始字节、NUL。路径统一使用 `/`，按 Unicode 码点排序。

## 限制

本次是文档结构和约束治理，不实施应用、不验证技术选型或媒体支持。链接检查不访问外部网络，也不证明语义与用户确认真实；这些通过阅读正本和会话答复核对。当前没有远端 CI 或自动合并保护。

# 待决事项

核对日期：2026-09-20。仅保留尚未决定的内容；已确认决定及 A 编号索引见 [确认记录](confirmed-decisions.md)。明确的产品优先级、基础库、目录、格式、录音交互、Bangumi/BitTorrent、中文 UI 和 Apache-2.0 已进入各自正本，不再列为待答问题。

按 A-20 和[连续执行流程](../dev-rules/development-workflow.md#continuous-execution)，本台账不是开工前必须全部回答的问卷。各项按所列截止阶段执行；先完成不依赖决定的实现和证据，非阻塞问题汇入最终审查，只有当前动作确实依赖且不可合理隔离时提前询问。

<a id="q-05"></a>
## Q-05：音频默认策略与精度

- 负责人：产品负责人、技术负责人。
- 尚需：录音期间媒体默认降音或暂停、保留选项的初始默认值及失败/取消时临时音频的清理时机、外放回声预期和允许定位误差。
- 已确认 OpenAI 格式转录与独立 BYOK；测试配置按 A-29 读取环境变量，文档不指定服务/模型默认值，必要测试缺配置时才询问。A-30 已确认长录音上传前筛除无人声、可选保留/不保留音频、分段回顾和采集时位置映射；这些需求不再列为待决。录音模式和悬浮框行为按需求实施。
- 截止：M1b 录音发布前。原型使用明确临时配置，不自动删除用户录音。开发者先补真实媒体身份、采样起点与设备恢复测量入口，再完成耳机、串音、断开与硬件偏移的[人工验证](../evidence/2026-09-19-m0-human-checklist.md)。当前软件联合日志不构成完整校准工具。

<a id="q-06"></a>
## Q-06：第三方插件执行形式（A-06）

- 负责人：技术负责人；状态：待隔离验证。
- 截止：M4 开放第三方执行前；先支持内置与受信任开发插件、可导出数据。Node 子进程不构成安全隔离证明。

<a id="q-08"></a>
## Q-08：主题与视觉数值

- 负责人：产品/设计负责人；状态：已授权先形成可调整的一版字体/主题，最终自有视觉数值待评审（A-25）。
- 白灰粉方向与中文首版已确认；仍需确定主题数量、具体 Token 和窗口尺寸实测基线。
- 截止：M1 视觉基线发布前；系统字体栈、HarmonyOS Sans SC 可选字体与可调整浅色主题先按语义 Token 验证，后续替换自有视觉。具体来源记录只保留本地，不进入公开文件；不为已授权原型路线重复询问。

<a id="q-09"></a>
## Q-09：分发与协作补充安排

- 负责人：项目维护者；Apache-2.0 已落地，Git/PR/CI 按 [ADR-0002](0002-local-first-github.md)执行。
- 尚需：正式产品安装形式、发布签名与分发渠道；是否额外采用贡献协议；GitBook 接入时的 Space、可见性和发布域名。
- 截止：相应分发、额外贡献安排或文档站接入前。未决内容不阻塞已授权的本地开发，不能据此推导推送、合并或发布授权。

<a id="q-10"></a>
## Q-10：Game、云同步与协作（A-10）

- 负责人：产品负责人；状态：vNext 待需求。
- 截止：启动对应功能设计前。另定义用户旅程、数据/权限和验收，首版无隐式依赖。

## 后续按阶段处理的技术选择

| 事项 | 推进与截止 |
| --- | --- |
| 可配置目录的启动定位机制 | 路径需求已定；M1a 验证平台初始化顺序、全量重定位与恢复。不默许固定的不可配置数据副本 |
| 解析器、视频/字幕后端与格式子特性 | 用户格式目标已定；M1b/M2 实现前用 POC-01/03 验证 DRM、版式、编码等限制，不以原型拒绝记录撤销目标 |
| BitTorrent 引擎、资源来源及做种策略 | 传输方式已定；M4 接入前确认恢复格式、做种/带宽默认及可使用来源；HTTP 原型不能替代 BT 验收 |
| Bangumi 字段策略与认证细节 | 站点已定；M2 接入时验证官方接口、字段语言、覆盖、限流与认证恢复 |
| 画布与后续服务 | 到对应阶段以需求和证据选型；不为尚无使用方的能力提前引入依赖 |

新增决定直接同步职责正本、验收和确认索引，再移除对应问题或已解决部分。实现遇到会改变产品含义、授权或不可逆结果的缺失决定时才询问；范围内的可逆实现与验证继续推进。

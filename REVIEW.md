# MANGA 评审口径

供人工和 AI reviewer 共用。先按 [AGENTS.md](AGENTS.md)定位本次变更的有效规则，再看完整相关实现与验收，不能只凭 diff 或通用偏好判断。当前没有自动 PR reviewer 接线；已有 CI 的实际范围见[质量门禁](docs/dev-rules/quality-gates.md)，不能用 CI 代替本评审。

## 评审顺序

1. 核对目标、需求 ID 和决定来源。草案、例子、未选型候选是否被误写成既成事实？已授权的新决定应同步替换旧结论。
2. 检查产品完整流程、错误状态和既有数据；MANGA 特别关注稳定引用、无 AI 可用、模块真实停用、上下文范围、异步结果目标和元数据换源。
3. 检查公共契约、依赖方向、调用权限、数据迁移、备份与恢复。涉及下载时区分传输完成、文件发布和入库完成。
4. 检查 UI 的输入法、键盘、焦点、窄窗和真实保存/取消状态；设计数值按当前正本核对。
5. 核对验证命令、执行版本和证据是否支持声明。规则与设计冲突须具体定位；仅文档检查通过不证明应用正确。

## 严重度

| 标记 | 判断 | 处理 |
| --- | --- | --- |
| P0 | 已证实的严重数据损坏、凭据泄漏或大范围不可用 | 立即处理，阻止对应交付或发布 |
| P1 | 核心流程失败、越权、引用错误、迁移或停用破坏承诺 | 修复并重新验证后交付 |
| P2 | 可复现的局部行为错误、有效契约冲突、误导实现的文档遗漏 | 修复；若延期，登记影响、负责人、期限与决定依据，不能与当前阶段必须项冲突 |
| 建议 | 风格、可选重构、没有证据的优化意见 | 可讨论，不当作缺陷或阻断项 |

一条有效 finding 应包含位置、具体触发条件、影响、适用规则或需求，以及可验证的修复方向。不要虚构“CI 已覆盖”或把缺少环境当作产品缺陷；重复的机器报错汇总到检查结果，不制造重复评论。

未发现问题时如实说明评审范围与未验证部分。修复后只对受影响范围和新风险复查；不为了凑问题扩大任务。

在已授权的审查修复任务中，发现可确定修复的问题直接处理、自测并继续，不逐个 finding 要求用户批准。按[集中审批流程](docs/dev-rules/development-workflow.md#continuous-execution)把确需产品决定、配置、人工验收或额外外部操作授权的内容汇成一份清单，提供具体推荐与证据。仅在缺失决定确实阻塞当前动作时提前询问，并继续不依赖它的审查与修复。

按 [A/B 交接规则](docs/dev-rules/development-workflow.md#agent-handoff)核对同一计划、B 的交付 commit 与实际证据。A 能确定的局部修复在本轮完成；较大实现缺口才集中交回 B，使用稳定问题编号、具体复现、预期与验收，不每发现一条即返工。B 按编号补修复/验证证据后 A 才关闭；新增愿望或风格建议不冒充原任务缺陷。

审查修复的版本保存按 [Git 规则](docs/dev-rules/git-and-github.md#review-commits)：同一交付未 push 时 amend 原提交，消息描述最终结果；不同里程碑或独立交付保留各自提交。报告保留问题与复验依据，不以额外修复 commit 留存本地审查过程；已推送历史的改写仍需明确授权。

审查结束按[下一步建议](docs/dev-rules/development-workflow.md#next-step)明确选择人工验收、给 B 的返工 prompt、完成阶段后的远端操作建议或下一阶段规划；注明理由和条件。A 审查通过不自动授权 push、创建 PR 或合并，必须阶段完成且用户明确表示执行相应动作。
